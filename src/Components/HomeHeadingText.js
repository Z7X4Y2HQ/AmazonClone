import React from 'react'
import {Text} from 'react-native'

export default HomeHeadingText = ({text}) => (
    <Text style={{ marginHorizontal: 16, margin: 16, fontSize: 18 }}>
      {text}
    </Text>
)